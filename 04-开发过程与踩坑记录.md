# 开发过程与踩坑记录

## 扫码确认了，应用仍显示未登录

### 表现

手机端已经完成扫码确认，Android 应用退出登录页后仍显示“尚未登录”。

### 原因方向

“用户在手机上点击确认”与“本地应用已经获得可用会话”是两个不同状态。仅凭页面跳转或用户操作无法判断最终登录结果。

### 调整

登录页增加明确的成功判定。只有本机确认会话完整且可用后，才显示成功提示、返回首页并更新账号卡片；失败或超时时继续提示重新扫码。

### 得到的经验

所有跨设备登录流程都需要区分“用户已操作”“服务端已确认”和“本机已保存”三个阶段。

## 能登录，但读不到动态

### 表现

账号卡片显示正常，使用其他账号发布测试内容后，应用没有记录。

### 排查方向

先把自动执行完全关掉，只保留手动读取与预览，分别检查：

- 会话是否仍然有效。
- 返回内容是否为空。
- 页面结构是否变化。
- 解析器是否识别到了目标字段。
- 内容是否被筛选条件提前排除。

### 调整

增加只读预览，让登录、读取、解析和自动执行可以分阶段验证，避免所有问题都表现为“没有动作”。

## 首次运行可能误处理历史内容

### 风险

应用刚启动时无法自然区分“刚出现的新内容”和“列表中原本存在的旧内容”。

### 调整

第一次读取只保存当前内容作为基线，不执行任何操作。只有之后出现的新目标才进入筛选流程。

### 得到的经验

自动任务恢复运行时，先建立安全起点比立刻补做任务更重要。

## 网络超时可能造成重复动作

### 风险

请求已经到达服务端，但本机没有收到明确结果。若直接重试，同一个目标可能被重复提交。

### 调整

不明确结果不自动重发，并保存本地处理记录。后续读取只负责核实状态，不继续“赌一次”。

## 后台运行被系统清理

### 表现

应用前台测试正常，熄屏或长时间不操作后停止检查。

### 调整方向

使用 Android 前台服务与常驻通知，并提示使用者允许后台活动、关闭针对该应用的电池优化。

### 保留限制

不同厂商系统的后台策略差异很大，应用无法保证永远不被系统回收，因此状态展示和恢复入口同样重要。

## 邮件测试失败

### 常见原因

- SMTP 服务尚未开启。
- 填入了邮箱登录密码，而不是独立授权码。
- 收件地址错误或邮件进入垃圾箱。
- 当前网络无法连接邮件服务。

### 调整

把邮件配置做成可选功能，并提供单独的测试按钮。手动暂停不发送提醒；仅异常自动暂停时发送一次。

## 这次开发留下的核心经验

1. 先做只读验证，再做写入操作。
2. 每个自动步骤都必须有明确的停止条件。
3. 登录成功需要本机证据，不能依赖界面感觉。
4. 网络不确定性必须进入状态模型。
5. 日志既要能排错，也要避免泄露敏感信息。
